import { User } from "../../utils/types";

type Props = {
    users: User[]
}

const UserList: React.FC<Props> = ({users}) => {
    return (
        <div>
            
        </div>
    )
}

export default UserList;